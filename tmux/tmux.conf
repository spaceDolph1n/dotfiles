source-file ~/.config/tmux/tmux.reset.conf

# `screen-256color` has no Smulx capability, so Neovim's undercurl diagnostics
# rendered as plain underlines inside tmux -- and it is why termguicolors has to
# stay pinned in the Neovim config. `tmux-256color` advertises both.
set-option -g default-terminal 'tmux-256color'
# `terminal-features` is the tmux 3.2+ replacement for terminal-overrides, and
# unlike the old ',xterm-256color:RGB' it is not conditional on the outer TERM.
set-option -as terminal-features ',*:RGB'
set-option -as terminal-features ',*:usstyle'

set -g prefix ^A
set -g base-index 1              # start indexing windows at 1 instead of 0
set -g detach-on-destroy off     # don't exit from tmux when closing a session
set -g escape-time 0             # zero-out escape time delay
set -g history-limit 1000000     # increase history size (from 2,000)
set -g renumber-windows on       # renumber all windows when any window is closed
set -g set-clipboard on          # use system clipboard
set -g status-position top       # macOS / darwin style

setw -g mode-keys vi
set -g pane-active-border-style 'fg=magenta,bg=default'
set -g pane-border-style 'fg=brightblack,bg=default'
set -g mouse on

set -g @plugin 'tmux-plugins/tpm'

set -g @plugin 'omerxx/catppuccin-tmux'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'
# No tmux-resurrect / tmux-continuum. Sessions are created on demand by sesh
# (below) and allowed to die; recreating one is a keystroke and a second, so
# persisting layouts to disk mostly persisted stale state -- and continuum's
# auto-restore competed with sesh over what should exist at attach time.
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin 'fcsonline/tmux-thumbs'
set -g @plugin 'wfxr/tmux-fzf-url'
# Supplies the C-h/C-j/C-k/C-l passthrough bindings that pair with the
# vim-tmux-navigator plugin already loaded in Neovim. Without it the Neovim side
# had nothing to hand off to, so crossing a pane boundary silently did nothing.
set -g @plugin 'christoomey/vim-tmux-navigator'

# ---------------------------------------------------------------------------
# Project switching
# ---------------------------------------------------------------------------
# Two different things, deliberately on two keys:
#
#   prefix + O   project -> WINDOW in the current session. One session, one
#                numbered window per project (dotfiles #1, web #2, ...).
#                Reuses the window if it already exists.
#   prefix + o   project -> SESSION (sesh). Separate sessions, switched between.
#
# display-popup -E gives the picker a real terminal, which `run-shell` does not.
bind-key "O" display-popup -E -w 80% -h 70% "$HOME/.config/scripts/tmux-project-window"

# ---------------------------------------------------------------------------
# Session switching: sesh (replaces omerxx/tmux-sessionx)
# ---------------------------------------------------------------------------
# sesh is a standalone binary rather than a tmux plugin, so the same session
# picker works from a bare shell too. It keeps the zoxide-backed behaviour.
#
# The old sessionx config also pointed @sessionx-custom-paths at
# /Users/spacedolphin/.config/dotfiles/ -- a username that does not exist on
# this machine, so that entry never resolved.
bind-key "o" run-shell "sesh connect \"$(
	sesh list --icons | fzf-tmux -p 80%,70% \
		--no-sort --ansi --border-label ' sesh ' --prompt '⚡  ' \
		--header '  ^a all  ^t tmux  ^g configs  ^x zoxide  ^d kill' \
		--bind 'tab:down,btab:up' \
		--bind 'ctrl-a:change-prompt(⚡  )+reload(sesh list --icons)' \
		--bind 'ctrl-t:change-prompt(🪟  )+reload(sesh list -t --icons)' \
		--bind 'ctrl-g:change-prompt(⚙️  )+reload(sesh list -c --icons)' \
		--bind 'ctrl-x:change-prompt(📁  )+reload(sesh list -z --icons)' \
		--bind 'ctrl-d:execute(tmux kill-session -t {2..})+change-prompt(⚡  )+reload(sesh list --icons)'
)\""

#catppuccin config
# NOTE: British spelling. The plugin reads `@catppuccin_flavour`; `@catppuccin_flavor`
# is silently ignored and it falls back to its "mocha" default. That typo was
# invisible for as long as the value *was* mocha.
set -g @catppuccin_flavour "kanso"
set -g @catppuccin_window_left_separator ""
set -g @catppuccin_window_right_separator " "
set -g @catppuccin_window_middle_separator " █"
set -g @catppuccin_window_number_position "right"
set -g @catppuccin_window_default_fill "number"
set -g @catppuccin_window_default_text "#W"
set -g @catppuccin_window_current_fill "number"
set -g @catppuccin_window_current_text "#W#{?window_zoomed_flag,(),}"
set -g @catppuccin_status_modules_right "directory date_time"
set -g @catppuccin_status_modules_left "session"
set -g @catppuccin_status_left_separator  " "
set -g @catppuccin_status_right_separator " "
set -g @catppuccin_status_right_separator_inverse "no"
set -g @catppuccin_status_fill "icon"
set -g @catppuccin_status_connect_separator "no"
set -g @catppuccin_directory_text "#{b:pane_current_path}"
set -g @catppuccin_date_time_text "%H:%M"

# The catppuccin fork loads its flavour file from inside its own plugin
# directory, so the custom Kanso theme has to live there. Re-link it on every
# tmux start -- otherwise `prefix + U` (plugin update) wipes the manual symlink
# from the README and the status bar silently reverts.
# TPM installs into $XDG_CONFIG_HOME/tmux/plugins when tmux.conf lives there --
# NOT ~/.tmux/plugins, which only holds tpm itself. Linking into the latter fails
# silently, so resolve the real path from TPM's own env var and fall back.
run-shell 'ln -sf ~/.config/dotfiles/tmux/catppuccin-kanso.tmuxtheme "${TMUX_PLUGIN_MANAGER_PATH:-$HOME/.config/tmux/plugins/}/catppuccin-tmux/"'

run '~/.tmux/plugins/tpm/tpm'

# ---------------------------------------------------------------------------
# Overrides — must stay AFTER tpm, or plugins win
# ---------------------------------------------------------------------------
# tmux-yank binds `y` and MouseDragEnd1Pane itself, so the same lines placed in
# tmux.reset.conf (sourced on line 1) were silently overwritten. Copy already
# worked -- what was missing was any confirmation that it had.
#
# -d 1000 overrides the global display-time of 4000ms: four seconds of "copied"
# is noise, one second reads as confirmation.
# The helper copies AND reports the size; the earlier version used a nerd-font
# glyph that may not render, and said nothing about how much was copied.
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel '~/.config/dotfiles/scripts/tmux-copy'
bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel '~/.config/dotfiles/scripts/tmux-copy'

# Confirmation should read as a quiet toast, not an alert. Left-aligned so it
# starts where the eye already is, rather than centred across the whole bar.
# `fill=` is REQUIRED as of tmux 3.7. From the man page: "The message is drawn on
# top of the existing status line... When the width is less than the full terminal
# width, the normal status bar content remains visible around the prompt area. The
# fill attribute is used by the default message-format to clear the background."
#
# Without it the prompt renders as a text-width box that grows as you type, with
# the window names showing around it — which is what broke on the 3.7 upgrade.
# `bg=` alone is not enough; fill is what clears the rest of the line.
set -g message-style "fg=#090E13,bg=#8ea4a2,align=left,fill=#8ea4a2"
set -g message-command-style "fg=#C5C9C7,bg=#22262D,align=left,fill=#22262D"

# ---------------------------------------------------------------------------
# Breathing room below the status bar
# ---------------------------------------------------------------------------
# tmux has no status padding option -- vertical space only comes from whole
# extra status LINES (`status 2..5`, tmux 2.9+), so one row is the smallest
# gap there is. With status-position top the lines render downwards, meaning
# the bar stays on [0] (tmux's untouched default, so mouse click-to-select and
# bell/activity styling are unaffected) and [1] is the blank row below it.
#
# The blank row fills with the same #090E13 the theme and WezTerm use as
# background, so it reads as whitespace, not as a taller bar.
#
# The gap ABOVE the bar is not here: a row would be far too much, so it comes
# from wezterm's window_padding.top, which is in pixels and can be a fraction
# of a row.
set -g status 2
# set -g 'status-format[1]' ""
